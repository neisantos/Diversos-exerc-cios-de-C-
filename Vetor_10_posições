*/ Faça um programa que leia um vetor de 10 posições com números inteiros positivos. 
Esse programa deverá ter uma função que retorne o maior número do vetor e outra 
função que retorne a média dos números do vetor.
*/

namespace QUESTÃO_01
{
    class Program
    {
        static void Main(string[] args)
        
        {
            int [] vetNumero = new int [3];
            int [] vetMaior = new int [3];
            int [] vetMedia = new int [3];
        
           Console.WriteLine("preencha o vetor de 10 posições !");

           for (int i = 0; i < vetNumero.Length;i++)
           {
                Console.Write($"digite um número na posição {i}  do vetor : ");
                vetNumero[i] =int.Parse(Console.ReadLine());                
           }
           Console.WriteLine("");
           for (int i = 0; i < vetNumero.Length;i++)
           {
               vetMedia [i] = 
           }
            
           for (int i = 0; i < vetNumero.Length;i++)
           {
               Console.Write($"{vetNumero[i]} |");
           }
           for (int i = 0; i < vetNumero.Length;i++)
           {
               
           }
                           
           





